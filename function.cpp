#include <iostream>
#include <math.h>
using namespace std;

/* bool isprime(int num)
{
    for (int i = 2; i < sqrt(num); i++)
    {
        if (num % i == 0)
        {
            return false;
        }
    }
    return true;
} */

// fibonaci sequence
/* void fib(int n)
{
    int t1 = 0, t2 = 1;
    int nextTerm = 0;
    for (int i = 1; i <= n; i++)
    {
        cout << t1 << endl;
        nextTerm = t1 + t2;
        t1 = t2;
        t2 = nextTerm;
    }
    return;
}
 */

// factorial sequence
/* int fact(int n)
{
    int factorial = 1;
    for (int i = 2; i <= n; i++)
    {
        factorial *= i;
    }
    return factorial;
} */

/* int sum(int n)
{
    int ans = 0;
    for (int i = 1; i <= n; i++)
    {
        ans += i;
    }
    return ans;
} */
int main()
{
    /*  int a, b;
     cin >> a >> b;
     for (int i = a; i < b; i++)
     {
         if (isprime(i))
         {
             cout << i << endl;
         }
     } */

    int n;
    cin >> n;
}